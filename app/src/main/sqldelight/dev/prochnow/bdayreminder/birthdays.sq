import dev.prochnow.bdayreminder.domain.entity.CategoryEntity;


selectAll:
SELECT *
FROM birthday JOIN category USING(category);

CREATE TABLE category(
    category TEXT AS CategoryEntity PRIMARY KEY NOT NULL
);

CREATE TABLE birthday(
    uuid TEXT PRIMARY KEY,
    personName Text NOT NULL,
    birthDay INTEGER AS Int NOT NULL,
    birthMonth INTEGER AS Int NOT NULL,
    birthYear INTEGER AS Int,
    category TEXT AS CategoryEntity NOT NULL,
    FOREIGN KEY(category) REFERENCES category(category)
 );
