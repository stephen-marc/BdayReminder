import dev.prochnow.bdayreminder.domain.entity.CategoryEntity;
import java.time.LocalDate;


selectAll:
SELECT * FROM birthday ORDER BY birthday.date;

insertFullBirthdateObject:
INSERT OR REPLACE INTO birthday(uuid, personName, date, category)
VALUES ?;

deleteBirthdate:
DELETE
FROM birthday
WHERE uuid = ?;

CREATE TABLE birthday(
    uuid TEXT PRIMARY KEY,
    personName Text NOT NULL,
    date TEXT AS LocalDate NOT NULL,
    category TEXT NOT NULL
 );
